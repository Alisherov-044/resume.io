import { NavbarDocumentCategoryCardInterface } from "@/interface";

export function NavbarDocumentCategoryCard({
  icon,
  label,
  description,
  link,
  className,
}: NavbarDocumentCategoryCardInterface) {
  return <h1>nav docs category card</h1>;
}
